---
name: 한글/한자 혼용 금지
description: 코드와 문서에서 분析(한자 析 포함) 사용 금지, 반드시 분析(한글)으로 작성
type: feedback
---
코드, 주석, 마크다운 문서에서 한자 `析`이 포함된 `分析` 또는 `분析`(한자 혼용)을 절대 사용하지 말 것.

**Why:** 터미널/UI에서 한자가 깨져 보이는 문제 발생. 사용자가 직접 수정 요청함.

**How to apply:** 항상 순수 한글 `분석`으로만 작성. grep으로 `析` 검색 시 결과가 없어야 정상.
