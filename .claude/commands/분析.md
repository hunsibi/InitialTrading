---
description: InitialTrading 주간 퀀트 파이프라인 실행 → 안정TOP5 + 모멘텀TOP5 출력 + 텔레그램 전송
allowed-tools: [Bash, Read]
---

# /분析 — 주간 퀀트 분析 파이프라인 (안정 대장주 + 모멘텀 이중 모델)

D:\WorkSpace\InitialTrading 의 주간 분析 파이프라인을 실행하고 두 모델 결과를 보여주는 커맨드.

파이프라인 순서: 데이터 업데이트 → 두 모델 분析 → 리포트 생성 → **텔레그램 전송**

---

## 1단계: 시작 안내

사용자에게 먼저 출력한다:
> 분析을 시작합니다... (약 3~5분 소요)
> 데이터 업데이트 → 안정 대장주 + 모멘텀 분析 → 리포트 생성 → 텔레그램 전송 순으로 진행됩니다.

## 2단계: 파이프라인 실행

```bash
cd D:/WorkSpace/InitialTrading && python run_pipeline.py 2>&1
```

**출력 결과 판단:**

- `완료! 텔레그램을 확인하세요` 포함 → **성공** → `pipeline_ok = True` 기록 후 3단계로
- `MongoDB에 연결할 수 없습니다` 포함 → **MongoDB 미실행**:
  > ⚠️ MongoDB 서비스가 실행 중이지 않습니다.
  > 파이프라인을 실행하려면 PC에서 `2_weekly.bat`를 실행해주세요.
  > 아래는 가장 최근에 저장된 분析 데이터입니다.

  → 3단계(기존 데이터 fallback)로
- 그 외 오류 → 오류 내용 간략 보고 후 3단계로

## 3단계: 데이터 파싱

```bash
cd D:/WorkSpace/InitialTrading && python parse_report.py 2>&1
```

출력 첫 줄이 `NO_DATA` 이면: "아직 분析 데이터가 없습니다. PC에서 `2_weekly.bat`를 먼저 실행해주세요." 안내 후 종료.

파싱 출력 형식:
```
DAYS_OLD=N
DATE=YYYY-MM-DD
KS=수익률%|상승수|하락수
KD=수익률%|상승수|하락수
S0=종목명|코드|시장|업종|현재가|주간수익률|12주수익률|RSI|거래량배수|매수가|손절가|손절%|목표①|목표①%|목표②|목표②%|이유1;;이유2;;이유3
S1=... S2=... S3=... S4=...   (안정 대장주 모델 TOP5)
M0=종목명|코드|시장|업종|현재가|주간수익률|12주수익률|RSI|거래량배수|매수가|손절가|손절%|목표①|목표①%|목표②|목표②%|이유1;;이유2;;이유3
M1=... M2=... M3=... M4=...   (단기 모멘텀 모델 TOP5)
E0=ETF명|코드|시장|테마|현재가|주간수익률|12주수익률|RSI|거래량배수|매수가|손절가|손절%|목표①|목표①%|목표②|목표②%|이유1;;이유2;;이유3
E1=... E2=... E3=... E4=...   (ETF 추천 모델 TOP5, ETF 모델 실패 시 빈 값)
```

DAYS_OLD ≥ 7 이고 파이프라인도 실패했다면, 결과 상단에:
> ⚠️ [N]일 전 데이터입니다.

## 4단계: 결과 출력

코드블록이나 표 없이 아래 형식으로 출력한다.
`pipeline_ok = True` 이면 마지막에 "텔레그램으로도 전송 완료 📱" 추가.

---

## 출력 형식

**📊 [DATE] 주간 퀀트 분析**

**이번 주 시장**
KOSPI [수익률] · 상승 [N] / 하락 [N]
KOSDAQ [수익률] · 상승 [N] / 하락 [N]

---

**🟢 안정 대장주 TOP 5** _(시총 상위 · 추세건전성+리스크조정)_

1️⃣ **[종목명]** [코드] · [시장] · [업종]
현재가 **[가격]원** | 주간 [수익률] | 12주 [수익률] | RSI [값] | 거래량 [배수]배
💰 매수 [가격]원 → 🛑 손절 [가격]원([%]) → 🎯 목표① [가격]원(+[%]) → 🏆 목표② [가격]원(+[%])
선정: [이유1] / [이유2]

(2️⃣~5️⃣ 동일 형식 반복)

---

**🔴 단기 모멘텀 TOP 5** _(전 종목 · 1주·4주·12주 모멘텀 · 고변동성 주의)_

1️⃣ **[종목명]** [코드] · [시장]
현재가 **[가격]원** | 주간 [수익률] | 12주 [수익률] | RSI [값] | 거래량 [배수]배
💰 매수 [가격]원 → 🛑 손절 [가격]원([%]) → 🎯 목표① [가격]원(+[%]) → 🏆 목표② [가격]원(+[%])
선정: [이유1] / [이유2]

(2️⃣~5️⃣ 동일 형식 반복)

---

**🔵 ETF 추천 TOP 5** _(레버리지·인버스 제외 · 추세+안정성 기준)_

E0~E4 파싱값이 있을 때만 출력. 없으면 "ETF 데이터 없음" 한 줄로 대체.

1️⃣ **[ETF명]** [코드] · [테마]
현재가 **[가격]원** | 주간 [수익률] | 12주 [수익률] | RSI [값] | 거래량 [배수]배
💰 매수 [가격]원 → 🛑 손절 [가격]원([%]) → 🎯 목표① [가격]원(+[%]) → 🏆 목표② [가격]원(+[%])
선정: [이유1] / [이유2]

(2️⃣~5️⃣ 동일 형식 반복)

---
⚠️ 퀀트 모델 자동 산출 결과 — 투자 손익 책임은 본인에게 있습니다
